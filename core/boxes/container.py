from core.models import ContainerBox

# In this module we re-export ContainerBox or customize container boxes if needed.
# By default, ContainerBox handles recursively parsing children of all standard container type boxes.
